# YouTube Workflow Portable 用户指南

这是免安装的 Windows 文件夹版本。它可以在本机完成 YouTube 视频下载、英文字幕选择、
Whisper 识别、AI 中文字幕、双语成片，以及可选的哔哩哔哩投稿。

开始前只需要记住四件事：

1. 必须先把 ZIP **完整解压**，不要在压缩包预览窗口中运行；
2. 推荐解压到较短的本地路径，例如 `D:\YouTubeWorkflow`；
3. 双击 `START_HERE.bat` 启动，不需要管理员权限；
4. 只处理你拥有、已获许可或明确有权使用的内容。

正常使用只需要浏览器里的控制面板。不要修改 `src`、`runtime` 或 `config`，也不要手工
删除任务目录中的 JSON 和检查点。

快速导航：
[选择 CPU / GPU](#选择-cpu-还是-gpu-版) ·
[启动](#启动) ·
[第一个视频](#三分钟完成第一个视频) ·
[首次配置](#首次配置) ·
[字幕与成片](#字幕与成片) ·
[无人值守](#无人值守自动化) ·
[哔哩哔哩投稿](#投稿哔哩哔哩) ·
[常见问题](#常见问题)

## 选择 CPU 还是 GPU 版

| 版本 | 适合谁 | Whisper 模式 | 额外要求 |
|---|---|---|---|
| Portable CPU | 没有 NVIDIA 显卡，或希望兼容性优先 | `cpu + int8` | 建议至少 16 GB 内存；长视频识别可能较慢 |
| Portable GPU | 有 NVIDIA 显卡，希望加快英文识别 | `cuda + float16` | 需要兼容的 NVIDIA 驱动 |

两个版本都包含完整的 faster-whisper large-v3 模型。GPU 包已经包含 CUDA 和 cuDNN Python
运行库，不需要另外安装 CUDA Toolkit；它仍然需要正常工作的 NVIDIA 驱动。

两个版本生成的任务目录和输出格式相同。成片时都会自动检测 NVENC，不可用时回退到 CPU
x264。无法确定时先使用 CPU 版；如果 GPU 版只在 Whisper 阶段报 CUDA 错误，也可以换
CPU 版验证其余流程。

## 启动

1. 右键 ZIP，选择“全部解压”；
2. 打开解压后的文件夹；
3. 双击 `START_HERE.bat`；
4. 等待默认浏览器打开 <http://127.0.0.1:8765>。

程序已经包含 Python 3.11、FFmpeg、FFprobe、yt-dlp、Deno、Whisper 模型和 biliup，不需要
单独安装这些组件。

浏览器页面关闭后，后台任务仍会继续；启动脚本所在的命令窗口关闭时，面板会停止，并安全
终止当前子任务。再次启动并执行相同操作时，程序会读取检查点继续。

如果浏览器没有自动打开，请手动访问 <http://127.0.0.1:8765>。页面只监听本机，不会
自动发布到局域网或公网。

## 三分钟完成第一个视频

1. 双击 `START_HERE.bat`；
2. 在“视频 ID / URL”中粘贴 YouTube 链接；
3. 勾选“我确认拥有下载和使用这些视频的权利”；
4. 点击加入下载队列，等待任务卡显示下载完成；
5. 勾选这个视频任务；
6. 选择“AI API 翻译”或“YouTube 自动中文”；
7. 选择“硬字幕 MP4”“软字幕 MKV”“两种都生成”或“仅双语 ASS”；
8. 使用 AI 时勾选“允许调用所选 AI API”；
9. 点击“一键生成成片”。

如果只需要中英字幕，不想编码视频，点击“处理到双语字幕”。

所有耗时步骤都会保存进度。关闭软件、网络中断或处理失败后，重新选中相同任务并执行相同
操作即可继续，不需要重新下载，也不会重复请求已经翻译成功的字幕。

## 首次配置

第一次打开页面时，顶部会提示配置服务。所有项目都是按功能选配：

| 配置 | 什么时候需要 | 不配置的影响 |
|---|---|---|
| YouTube API Key | 关键词搜索、智能发现 | 仍可直接粘贴 URL / 视频 ID 下载 |
| YouTube Cookie | 登录验证、年龄限制、机器人检查 | 普通公开视频通常不受影响 |
| AI 翻译 API | AI 中文字幕、可选的云端投稿文案 | 可使用已有的 YouTube 自动中文 |
| Ollama | 本地智能发现、可选的本地投稿文案 | 智能发现会退回规则评分 |
| biliup 账号 | 投稿哔哩哔哩 | 下载、字幕和成片仍可使用 |

配置完成后，可以随时点击页面右上角“配置服务”修改。API Key 写入当前文件夹的 `.env`；
Cookie 复制到 `private\cookies.txt`；页面只显示配置状态，不会回显密钥原文。

### YouTube API Key

直接粘贴 URL 下载不需要 Key。只有关键词搜索和智能发现需要 YouTube Data API：

1. 创建或选择 Google Cloud 项目；
2. 启用 YouTube Data API v3；
3. 在“凭据”页面创建 API Key；
4. 回到控制面板保存并重新检测。

官方入口：[YouTube Data API 入门](https://developers.google.com/youtube/v3/getting-started) ·
[Google Cloud 凭据](https://console.cloud.google.com/apis/credentials)

### YouTube Cookie

普通公开视频通常不需要 Cookie。遇到登录、年龄或机器人验证时：

1. 在浏览器中登录下载时要使用的 YouTube 账号；
2. 只导出 `youtube.com` Cookie；
3. 文件格式选择 Mozilla/Netscape `cookies.txt`；
4. 在“配置服务 → YouTube Cookie”中选择 TXT 并保存；
5. 状态显示“已导入”后重新下载。

参考：[yt-dlp Cookie 说明](https://github.com/yt-dlp/yt-dlp/wiki/FAQ#how-do-i-pass-cookies-to-yt-dlp) ·
[YouTube Cookie 导出建议](https://github.com/yt-dlp/yt-dlp/wiki/Extractors#exporting-youtube-cookies)

Cookie 等同账号登录凭据，不要发送给任何人。Cookie 失效后重新导出即可。

### AI 翻译

在“配置服务 → AI 翻译”中选择供应商、模型和 Thinking，再填写对应 API Key。当前面板
支持 DeepSeek、智谱 GLM、通义千问、Kimi、MiniMax、豆包、OpenAI、Anthropic，以及
自定义 OpenAI 兼容接口。

Key 只保存在本机。免费额度、模型可用性、价格和限流由供应商决定；处理长视频前请先
检查账户余额。程序不会因为某家失败而自动切换到另一家可能收费的供应商。

默认配置为每批 32 条字幕、前后各 2 条上下文、最大输出 4096 Token、Thinking 关闭。
翻译会按批保存，失败后只请求待完成 ID。程序不会因为主观语言评分自动购买第二次翻译；
只有你明确选择整批润色或强制重译时才会产生对应调用。

### Ollama 智能发现（可选）

智能发现的本地 AI 功能默认使用：

```powershell
ollama pull qwen3.5:9b
ollama pull qwen3-embedding:0.6b
```

请先自行安装并启动 [Ollama](https://ollama.com/)。模型可在“配置服务 → Ollama 本地
智能发现”中更换或停用。Ollama 不可用时不会阻止软件启动，智能发现会给出警告并使用
规则评分。

### 哔哩哔哩账号（可选）

在配置窗口点击“打开登录工具”，使用 bbup/biliup 窗口登录自己的账号，再回到页面点击
“登录后重新检测”。也可以双击：

```text
login_bilibili.bat
```

账号文件保存在 `biliup\bbup-app\data`。已有的 biliup 账号 JSON 也可以放到
`private\biliup_accounts`。发布包默认不包含制作者账号。

## 下载视频

控制面板提供三种添加方式：

### 视频 ID / URL

每行粘贴一个 YouTube 地址或视频 ID。这是最简单的入口，不需要 YouTube API Key。

### 关键词搜索

输入关键词、数量和排序方式。搜索结果不会自动下载；勾选目标视频并确认使用权后，才会
进入下载队列。

### 智能发现

按领域和时间窗口召回候选，再用规则与可选的 Ollama 模型做评分、排序、去重和反馈学习。
默认领域包括 AI、新科技、软件编程、科学、游戏、Minecraft、化学、挑战实验、娱乐、
农业园艺、美食烹饪、户外旅行、教程技能、艺术创意和社会实验。

智能发现需要 YouTube API Key，并会消耗 YouTube Data API 配额。Ollama 只接收 YouTube
公开元数据和缩略图，不会收到 API Key、Cookie、本地视频或字幕。候选不足时允许少于
目标数量，不会为了凑数重新加入已拒绝、低热度或重复内容。

无论通过哪种方式添加，下载前都必须确认素材权利。默认保留最高 1080p 的原视频、源音频、
可用的中英文字幕、封面、标题、简介、视频 ID 和下载记录。后续翻译与成片不会覆盖原始文件。

## 字幕与成片

### 英文字幕

程序会在以下来源中选择：

1. YouTube 手工英文字幕；
2. YouTube 自动英文字幕；
3. 本地 Whisper 识别结果。

“自动英文字幕仍启用 Whisper 对比”默认开启。程序会同时准备 YouTube 与 Whisper 结果，
再根据覆盖率、时间轴、稳定性、可读性和来源可信度自动选择。关闭后，已有 YouTube 英文
字幕时不再额外识别；没有英文字幕时仍会用 Whisper 兜底。

### 中文字幕

- **AI API 翻译**：使用当前供应商和模型，开始前必须确认允许调用；
- **YouTube 自动中文**：不调用翻译 API，但视频必须存在可用的自动中文字幕。

AI 翻译只做结构验证：确保 JSON 可读、字幕 ID 完整、译文非空、时间轴不变、没有非法
控制字符，以及单个 ID 没有明显混入多条字幕。它不会用本地主观规则决定翻译好坏并自动
再次付费。

### 输出模式

| 模式 | 结果 | 适合场景 |
|---|---|---|
| 仅双语 ASS | `.ass` 字幕 | 预览、继续编辑，不编码视频 |
| 软字幕 MKV | 可开关字幕轨 | 归档和后期处理 |
| 硬字幕 MP4 | 字幕压入画面 | 直接播放或投稿 |
| 两种都生成 | MKV + MP4 | 同时保留归档与发布版本 |

成片保留原始配音。默认英文在上、中文在下，每种语言始终只占一行。长句会在安全范围内
缩小、轻微横向压缩或分页。

如果安全字号下仍会裁切，或分页后显示时间太短，任务会显示“需要复核”，只生成预览和
报告，不启动 FFmpeg。点击任务卡右侧黄色“审”按钮，可以：

1. 查看受影响的字幕编号、时间和中英原文；
2. 缩短“成片显示英文/中文”；
3. 或明确选择“忽略此条并继续生成”；
4. 保存并重新检查；
5. 全部通过后自动继续成片。

复核只创建成片专用副本，不修改原字幕、字幕 ID 或时间轴。选择忽略时，也只从成片显示
副本中移除该条。

### 中文 AI 配音（V1，可选）

默认 Portable 包不包含 VoxCPM2 大模型和配音专用依赖，因此不开启时原有流程完全不变。
需要此功能时，请在当前目录额外准备 `.venv_dubbing`（Python 3.11、匹配显卡的 PyTorch、
`requirements_dubbing.txt`）并把完整模型放到 `models\VoxCPM2`。控制面板会检查 Demucs、
VoxCPM2、本地模型和 CUDA；缺项时拒绝加入配音队列，不会静默下载 VoxCPM2 权重。

先用 AI 翻译生成 `zh.clean.srt`，或准备人工审核的 `zh.reviewed.srt`。勾选“启用中文 AI
配音”后，可自动选取 5–10 秒参考人声，或手工填写原视频时间段；配音成片可以只显示中文
或继续显示中英双语。此阶段只读取现有中文字幕，不运行 Whisper，也不自动翻译。

每句 TTS 都会单独保存，进程终止或某句失败后重试只继续缺失句；轻微超时会有限加速，
明显超时会标记复核，不会直接截断。V1 只支持单主播音色，不支持多人声分离。Demucs 首次
明确运行时可能准备自己的 `htdemucs` 模型缓存；VoxCPM2 权重必须由用户预先放到本地。

## 批量任务与断点续跑

任务卡按“下载 → 英文 → AI 翻译 → 配音 → 成片 → 投稿”显示状态；未启用配音时该阶段
为跳过。并行调度 v0.5 提供：

- 最多 2 个并发下载；
- 最多 2 个不同视频并发调用 AI API；
- 同一时间 1 个 Whisper / 成片重任务；
- 同一时间 1 个上传任务；
- Portable CPU 全局最多 3 个进程，Portable GPU 全局最多 4 个进程。

不同视频会交错使用资源，单个视频仍按依赖顺序处理。每个并发下载使用独立的临时 Cookie
副本，结束后自动删除。

- “查看日志”：显示当前步骤和详细错误；
- “终止”：停止当前进程，但保留已经生成的文件；
- “重试”：校验检查点，只继续未完成工作；
- “清空历史记录”：只删除已结束的作业记录和日志，不删除视频；
- “删除任务 / 批量删除”：永久删除对应视频的本地文件。

运行中或排队中的任务不能直接删除，必须先终止。不要用资源管理器手工删除任务目录中的
部分文件，否则可能破坏续跑记录。

## 无人值守自动化

“无人值守自动化”可以让新下载任务自动处理到：

- 双语字幕；
- 双语成片；
- 完整投稿。

你可以分别选择英文来源策略、中文来源策略、成片格式、无配音视频处理、异常处理、投稿
资料模型、投稿账号和可见性。

“始终 API 翻译”会忽略已有 YouTube 中文并为每个视频重新翻译；“只用 YouTube 中文”
不会调用翻译 API。完整投稿必须包含硬字幕 MP4。

普通的“AI 字幕尚未人工浏览”提示不会暂停无人值守任务，但字幕结构、时间轴或排版安全
检查失败时绝不会上传。默认异常策略会记录原因、跳过该视频并继续队列。自动化投稿默认
公开；想先私下检查时，请开启“仅自己可见”。

## 投稿哔哩哔哩

未开启无人值守时，完成硬字幕 MP4 后，任务卡会显示投稿按钮。投稿前请逐项核对：

- 账号、标题、标签、简介和分区；
- 自制或转载类型；
- 转载来源；
- 公开或仅自己可见；
- 禁止转载选项；
- 上传线路、并发数和封面。

首次测试建议勾选“仅自己可见”。如果硬字幕 MP4 尚未生成，投稿队列会先完成成片再上传。
成功后任务会记录 BV 号和链接，避免重复投稿。

默认最短投稿间隔为 10 分钟，每个本机自然日最多成功投稿 20 条；可在“配置服务 →
哔哩哔哩账号”调整。平台返回 `137022`“投稿过于频繁”时，当前任务会回到队列，后续
上传暂停 6 小时；重启软件不会绕过冷却，页面会显示预计恢复时间。

## 查看结果

点击视频任务右侧“打开任务目录”。以下路径均相对于单个视频任务目录：

| 相对路径 | 内容 |
|---|---|
| `video\source.mp4` | 下载的原视频 |
| `audio\source_audio.wav` | Whisper 使用的源音频 |
| `metadata\info.json` | 原视频元数据 |
| `metadata\description.txt` | 原视频简介 |
| `metadata\thumbnail.jpg` | 下载封面 |
| `subtitles\en.selected.srt` | 最终英文字幕 |
| `subtitles\zh.clean.srt` | 最终中文字幕 |
| `subtitles\zh.reviewed.srt` | 可选的人工审核中文字幕 |
| `dubbing\reference.wav` | 用于单主播音色克隆的参考人声 |
| `dubbing\segments\*.wav` | 可断点续跑的逐句中文 TTS |
| `dubbing\chinese_voice.wav` | 按原字幕绝对时间轴拼接的中文人声 |
| `dubbing\dubbed_audio.wav` | 与原背景声混合后的最终中文配音音轨 |
| `dubbing\manifest.json` | 逐句输入哈希、时长、失败与复核状态 |
| `stage4\subtitles\bilingual.ass` | 双语 ASS |
| `stage4\subtitles\bilingual_preview.ass` | 需要复核时的预览 |
| `stage4\video\final_bilingual_hardsub.mp4` | 硬字幕成片与默认投稿文件 |
| `stage4\video\final_bilingual_softsub.mkv` | 软字幕成片 |
| `stage4\video\final_chinese_dubbed_hardsub.mp4` | 仅中文字幕的中文配音成片 |

下载、日志、队列状态和账号分别保存在根目录的 `downloads`、`logs`、`work`、`private`
和 `biliup\bbup-app\data`。这些目录属于你的本地数据。

## 常见问题

| 现象 | 处理方法 |
|---|---|
| 双击后窗口立即关闭 | 确认 ZIP 已完整解压；把目录移到 `D:\YouTubeWorkflow` 一类短路径；检查安全软件是否隔离文件 |
| 浏览器没有自动打开 | 手动访问 <http://127.0.0.1:8765> |
| Windows 阻止运行下载的脚本 | 先核对发布者和 ZIP 的 SHA256；不要在来源不明或哈希不匹配时绕过安全提示 |
| GPU 版提示 CUDA / 驱动错误 | 更新兼容的 NVIDIA 驱动，或改用 CPU 版 |
| YouTube 要求登录、年龄验证或机器人检查 | 导入有效 Cookie；失效后重新导出 |
| 下载中途出现 `HTTP 403` | 程序会保留分片并尝试嵌入式客户端续传；仍失败时更新 yt-dlp、切换网络，必要时参考 [PO Token 指南](https://github.com/yt-dlp/yt-dlp/wiki/PO-Token-Guide) |
| 下载完成但没有英文字幕 | 检查 Whisper 模型和运行版本；GPU 版还需检查驱动 |
| AI 翻译报 `401/403` | 检查 Key、Base URL、模型和账户权限 |
| AI 翻译报 `402` | 检查余额或计费状态 |
| AI 翻译报 `429/1305` | 供应商限流或拥堵；程序会保存检查点后等待，请不要反复强制重开 |
| 成片显示“需要复核” | 点击黄色“审”，缩短或明确隐藏受影响字幕；预检通过后自动继续 |
| 中文配音环境未就绪 | 检查 `.venv_dubbing`、PyTorch CUDA、NVIDIA 驱动、Demucs/VoxCPM2 包和 `models\VoxCPM2` |
| 中文配音中途失败 | 修复后点重试即可续跑；点“重新生成中配”才会强制重做全部句子 |
| 没有投稿按钮 | 确认硬字幕成片完成、没有排版复核，并已登录 biliup |
| 投稿显示 `137022` | 不要重复新建任务；等待页面显示的冷却结束 |
| 重新运行是否重复付费 | 配置和提示版本不变时会复用检查点，只请求缺失项；更换模型、Thinking 或强制重译会使相关检查点失效 |

## 更新、备份与转发

- 更新前先结束所有任务并关闭面板；
- 先完整备份旧文件夹，不要在活动任务运行时直接覆盖 `runtime`、`src` 或 `config`；
- 新版本建议解压到新的短路径，确认可以启动后再处理旧数据；
- 重要成片和字幕请另行备份，任务删除操作不可恢复；
- 给别人软件时发送原始发布 ZIP，不要重新压缩已经运行过的文件夹。

使用过的文件夹可能包含 `.env`、Cookie、哔哩哔哩账号、下载内容、任务日志和数据库，即使
表面上看不到密钥，也不适合直接转发。

## 隐私、安全与版权

- API Key 保存在当前目录 `.env`，控制面板不会回显密钥原文；
- YouTube Cookie 和哔哩哔哩账号文件等同登录凭据，不要发送给任何人；
- AI 翻译会把字幕文本和必要上下文发送给你选择的供应商；
- Ollama 智能发现只使用 YouTube 公开元数据和缩略图；
- 软件只监听本机 `127.0.0.1`，不会自动部署到公网；
- 删除任务是不可恢复操作，执行前确认视频 ID 和目录；
- 已经泄露的 Key 或 Cookie 应立即吊销、退出账号并重新创建；
- 只下载、处理和投稿你拥有、已获许可或明确有权使用的内容。

本工具不授予任何视频、音乐、字幕或平台内容的版权与使用权。所包含第三方组件的授权信息
见根目录 `THIRD_PARTY_NOTICES.md`。
