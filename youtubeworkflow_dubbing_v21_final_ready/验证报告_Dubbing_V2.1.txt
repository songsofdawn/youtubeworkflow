Dubbing V2.1 最终验证报告
=========================

核心功能
--------
1. Utterance Planner V2 / Semantic Boundary Repair
2. Timing Scheduler V2（soft 0.50s / hard 0.75s）
3. Canonical Duration Rewrite（最多 2 轮）
4. Reference Quality Gate
5. 付费 API 权限贯穿与硬保护

验证
----
- 修改模块 py_compile：通过
- V2.1 核心独立测试：23 passed, 2 skipped
- 此前 V2.1 相关 Stage3 + Dubbing：63 passed, 3 skipped
- Stage4：104 passed
- dubbing_config validate_dubbing_config：通过

沙箱限制
--------
完整 control_panel / model_reuse 测试在本沙箱无法 collection，因为用户此前的源码收集包
没有包含 src/discovery。用户完整本地仓库中存在该目录，因此请覆盖后运行完整 pytest。
这个 collection 错误不是 V2.1 代码失败。

真实 238-utterance manifest 的 scheduler 回放结论保持不变：
- 原始 max shift 约 3.165 s
- 第一轮目标达成模拟后约 0.782 s
- 第二轮目标达成模拟后约 0.764 s（hard 0.75 + overlap tolerance 0.02）
实际 GLM 若不能压缩到目标，系统会保留 review，不会伪造通过。
